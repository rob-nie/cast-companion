
{
  "rules": {
    "users": {
      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid"
      }
    },
    "projects": {
      ".read": "auth !== null",
      ".write": "auth !== null",
      "$projectId": {
        ".read": "auth !== null",
        ".write": "auth !== null || data.child('ownerId').val() === auth.uid"
      }
    },
    "projectMembers": {
      ".read": "auth !== null",
      ".write": "auth !== null"
    },
    "notes": {
      "$noteId": {
        ".read": "auth !== null",
        ".write": "auth !== null"
      }
    },
    "messages": {
      "$messageId": {
        ".read": "auth !== null",
        ".write": "auth !== null"
      }
    }
  }
}
