
{
  "rules": {
    "users": {
      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid"
      }
    },
    "projects": {
      ".read": "auth !== null",
      ".write": "auth !== null",
      "$projectId": {
        ".validate": "newData.child('ownerId').exists() && (data.child('ownerId').val() === auth.uid || !data.exists())"
      }
    },
    "projectMembers": {
      ".read": "auth !== null",
      ".write": "auth !== null"
    },
    "notes": {
      ".read": "auth !== null",
      ".write": "auth !== null"
    },
    "messages": {
      ".read": "auth !== null",
      ".write": "auth !== null"
    }
  }
}
